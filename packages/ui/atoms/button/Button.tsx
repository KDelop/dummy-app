import { FC } from 'react';

// styles
import { IButtonProps, btnType } from './Button.types';
import styles from './Button.styles';

const Button: FC<IButtonProps> = ({ variant, onClick }) => {
  return <div />;
};

export { Button };
